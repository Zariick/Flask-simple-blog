This is a Blog app using **Flask** features and **SQLAlchemy** on python
_____
### Setup
*Installing requirement libraries*

On Windows type:
```
python -m pip install -r requirements.txt
```
On MacOS type:
```
pip3 install -r requirements.txt
```
